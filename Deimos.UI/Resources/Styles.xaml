<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
                    xmlns:svg="clr-namespace:SharpVectors.Converters;assembly=SharpVectors.Converters.Wpf"
                    xmlns:models="clr-namespace:Deimos.UI.Models">

    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#101923"/>
    <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#122033"/>
    <SolidColorBrush x:Key="SurfaceBackgroundBrush" Color="#162334"/>
    <SolidColorBrush x:Key="ControlBorderBrush" Color="#294057"/>
    <SolidColorBrush x:Key="PrimaryTextBrush" Color="#E7EEF7"/>
    <SolidColorBrush x:Key="MutedTextBrush" Color="#A7B5C5"/>
    <SolidColorBrush x:Key="AccentBrush" Color="#A32138"/>
    <SolidColorBrush x:Key="AccentHoverBrush" Color="#C72A45"/>
    <SolidColorBrush x:Key="MenuItemHoverBrush" Color="#4DA32138"/>
    <SolidColorBrush x:Key="ButtonFaceBrush" Color="#E3EAF3"/>
    <SolidColorBrush x:Key="ButtonHoverBrush" Color="#F4F7FB"/>
    <SolidColorBrush x:Key="ButtonPressedBrush" Color="#C8D1DD"/>
    <SolidColorBrush x:Key="DarkTrackBrush" Color="#2A3B4F"/>
    <SolidColorBrush x:Key="ListHoverBrush" Color="#1E2D40"/>
    <SolidColorBrush x:Key="PlaybackSurfaceBrush" Color="#0E1824"/>

    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource WindowBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
    </Style>
    
    <!-- Title bar gradient -->
    <LinearGradientBrush x:Key="TitleBarBackgroundBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#132030" Offset="0"/>
        <GradientStop Color="#0F1823" Offset="1"/>
    </LinearGradientBrush>

    <!-- Menu separator gradient line -->
    <LinearGradientBrush x:Key="MenuSeparatorBrush" StartPoint="0,0.5" EndPoint="1,0.5">
        <GradientStop Color="#1A2B3C" Offset="0"/>
        <GradientStop Color="#2A415A" Offset="0.5"/>
        <GradientStop Color="#1A2B3C" Offset="1"/>
    </LinearGradientBrush>

    <Style x:Key="SectionBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource PanelBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ControlBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0.5"/>
        <Setter Property="CornerRadius" Value="2"/>
        <Setter Property="Padding" Value="0"/>
    </Style>

    <Style TargetType="Menu">
        <Setter Property="Background" Value="{StaticResource PanelBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
    </Style>

    <Style TargetType="MenuItem">
        <Setter Property="Background" Value="{StaticResource PanelBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="Padding" Value="8,3"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Grid>
                        <Border x:Name="Border" Background="{TemplateBinding Background}"
                                BorderBrush="Transparent" BorderThickness="0"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header" RecognizesAccessKey="True"/>
                        </Border>
                        <Popup x:Name="PART_Popup" Placement="Bottom" IsOpen="{TemplateBinding IsSubmenuOpen}"
                               AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
                            <Border Background="{StaticResource PanelBackgroundBrush}"
                                    BorderBrush="{StaticResource ControlBorderBrush}"
                                    BorderThickness="0.5" CornerRadius="2" Padding="0">
                                <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Border" Property="Background" 
                                    Value="{StaticResource MenuItemHoverBrush}"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsHighlighted" Value="True"/>
                                <Condition Property="Tag" Value="Destructive"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Border" Property="Background" 
                                    Value="{StaticResource AccentBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" 
                                    Value="{StaticResource MutedTextBrush}"/>
                        </Trigger>
                        <Trigger Property="Role" Value="TopLevelHeader">
                            <Setter Property="Padding" Value="10,4"/>
                        </Trigger>
                        <Trigger Property="Role" Value="SubmenuItem">
                            <Setter Property="Padding" Value="10,4"/>
                            <Setter TargetName="PART_Popup" Property="Placement" Value="Right"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="{x:Static MenuItem.SeparatorStyleKey}" TargetType="Separator">
        <Setter Property="Margin" Value="0,4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Border Height="1"
                            Background="{StaticResource MenuSeparatorBrush}"
                            HorizontalAlignment="Stretch"
                            SnapsToDevicePixels="True"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <Style x:Key="NowPlayingTextBlockStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <Style TargetType="GridSplitter">
        <Setter Property="Background" Value="#203040"/>
    </Style>

    <ControlTemplate x:Key="SliderThumbTemplate" TargetType="Thumb">
        <Border Width="16" Height="16" CornerRadius="8" 
                Background="{StaticResource AccentBrush}" 
                BorderBrush="White" BorderThickness="1"/>
    </ControlTemplate>

    <ControlTemplate x:Key="SliderRepeatButtonTemplate" TargetType="RepeatButton">
        <Border Height="8" CornerRadius="4" Background="{TemplateBinding Background}" 
                IsHitTestVisible="False"/>
    </ControlTemplate>

    <Style TargetType="Slider">
        <Setter Property="Height" Value="22"/>
        <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
        <Setter Property="Background" Value="{StaticResource DarkTrackBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid VerticalAlignment="Center" Background="Transparent">
                        <Track x:Name="PART_Track">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton IsHitTestVisible="False" Focusable="False" 
                                              Background="{TemplateBinding Foreground}" 
                                              Template="{StaticResource SliderRepeatButtonTemplate}"/>
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Template="{StaticResource SliderThumbTemplate}"/>
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton IsHitTestVisible="False" Focusable="False" 
                                              Background="{TemplateBinding Background}" 
                                              Template="{StaticResource SliderRepeatButtonTemplate}"/>
                            </Track.IncreaseRepeatButton>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ListView">
        <Setter Property="Background" Value="{StaticResource SurfaceBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ControlBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0.5"/>
    </Style>

    <Style x:Key="PlaylistItemStyle" TargetType="ListViewItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="Padding" Value="10,7"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource ListHoverBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="PlaylistMetaTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, 
            Path=IsSelected}" 
                         Value="True">
                <Setter Property="Foreground" Value="#F2F6FB"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <DataTemplate x:Key="PlaylistItemTemplate" DataType="{x:Type models:MediaFile}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="52"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Width="48" Height="48" CornerRadius="4" VerticalAlignment="Center"
                    Background="{StaticResource PlaybackSurfaceBrush}"
                    BorderBrush="{StaticResource ControlBorderBrush}" BorderThickness="0.5">
                <Image Source="{Binding ImagePath}" Stretch="UniformToFill"/>
            </Border>

            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="SemiBold"
                           Foreground="{Binding RelativeSource={RelativeSource 
                           AncestorType=ListViewItem}, Path=Foreground}"
                           TextTrimming="CharacterEllipsis"/>
                <TextBlock Text="{Binding Artist}" 
                           Style="{StaticResource PlaylistMetaTextStyle}"/>
                <TextBlock Text="{Binding Album}" 
                           Style="{StaticResource PlaylistMetaTextStyle}"/>
            </StackPanel>

            <TextBlock Grid.Column="3" Margin="12,0,0,0" VerticalAlignment="Center"
                       Style="{StaticResource PlaylistMetaTextStyle}"
                       Text="{Binding Duration, StringFormat={}{0:hh\\:mm\\:ss}}"
                       HorizontalAlignment="Right"/>
        </Grid>
    </DataTemplate>

    <Style TargetType="MediaElement">
        <Setter Property="Stretch" Value="Uniform"/>
    </Style>

    <Style x:Key="PlayerIconButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="44"/>
        <Setter Property="Height" Value="44"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="6,0"/>
        <Setter Property="Background" Value="{StaticResource ButtonFaceBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ControlBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource ButtonHoverBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource ButtonPressedBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SmallPlayerIconButtonStyle" TargetType="Button" 
           BasedOn="{StaticResource PlayerIconButtonStyle}">
        <Setter Property="Width" Value="38"/>
        <Setter Property="Height" Value="38"/>
    </Style>

    <!-- Title bar button style for minimize and maximize/restore -->
    <Style x:Key="TitleBarButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="46"/>
        <Setter Property="Height" Value="44"/>
        <Setter Property="Background" Value="#0F1823"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#16222E"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#243547"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Dedicated style for the close button -->
    <Style x:Key="CloseTitleBarButtonStyle" TargetType="Button" 
           BasedOn="{StaticResource TitleBarButtonStyle}">
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    
    <Style x:Key="PlayerIconStyle" TargetType="{x:Type svg:SvgViewbox}">
        <Setter Property="Width" Value="26"/>
        <Setter Property="Height" Value="26"/>
        <Setter Property="Stretch" Value="Uniform"/>
    </Style>

</ResourceDictionary>
